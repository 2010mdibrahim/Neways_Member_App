import 'package:flutter/material.dart';

const Color primaryColor =
    Color(0xFF4169E1); // 0xFF1796CD // Color.fromARGB(255, 3, 45, 117)
const Color bodyColor = Color(0xFFF2F2F2);
const double defaultPadding = 16.0;
const defaultPaddingEdge = EdgeInsets.all(defaultPadding / 2);
